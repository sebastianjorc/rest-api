{
    "version": 2,
    "builds": [
      {
        "src": "*.ts",
        "use": "@vercel/node-server",
        "config": {
          "command": "npx -tsc && node dist/src/app.js"
        }
      }
    ],
    "routes": [
      {
        "src": "/.*",
        "dest": "(.*)"
      }
    ]
  }
  